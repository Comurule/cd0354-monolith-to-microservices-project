language: node_js

services:
  - docker

# Pre-testing installs
install:
  - echo "nothing needs to be installed"

# Scripts to be run such as tests
before_script:
  - echo "no tests"

script:
  - docker --version # print the version for logging
  - docker-compose up

after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker push comurule/udagram-frontend
  - docker push comurule/udagram-api-feed
  - docker push comurule/udagram-api-user
  - docker push comurule/reverseproxy

